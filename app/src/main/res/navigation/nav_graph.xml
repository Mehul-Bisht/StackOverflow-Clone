<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/homeFragment">

    <action
        android:id="@+id/action_mainActivity_to_filterFragment"
        app:destination="@id/filterFragment" />

    <fragment
        android:id="@+id/homeFragment"
        android:name="com.example.stackoverflowclone.presentation.homeScreen.HomeFragment"
        android:label="fragment_home"
        tools:layout="@layout/fragment_home" >
        <action
            android:id="@+id/action_homeFragment_to_webViewFragment"
            app:destination="@id/webViewFragment" />
    </fragment>
    <dialog
        android:id="@+id/filterFragment"
        android:name="com.example.stackoverflowclone.presentation.filterScreen.FilterFragment"
        android:label="fragment_filter"
        tools:layout="@layout/fragment_filter" >
        <argument
            android:name="tags"
            app:argType="com.example.stackoverflowclone.domain.models.Tags" />
    </dialog>
    <fragment
        android:id="@+id/webViewFragment"
        android:name="com.example.stackoverflowclone.presentation.shared.WebViewFragment"
        android:label="fragment_web_view"
        tools:layout="@layout/fragment_web_view" >
        <argument
            android:name="url"
            app:argType="string" />
    </fragment>
</navigation>